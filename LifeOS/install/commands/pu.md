---
name: pu
description: Extract system improvements from content, monitor external sources, and scan bookmarks for upgrade opportunities. Shortcut for Upgrade skill (formerly PAIUpgrade).
argument-hint: [url or topic]
---

# pu — Redirect

This command routes to the **Upgrade** skill (the renamed and gap-aware successor to PAIUpgrade).

**Invoke the skill directly:**

Use the Skill tool to invoke Upgrade with the provided arguments:

```
Skill("Upgrade", "$ARGUMENTS")
```
